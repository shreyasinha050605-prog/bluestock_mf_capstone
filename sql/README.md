# SQL

This folder contains SQL files used for database creation and validation.

Files:

- schema.sql
- queries.sql

The scripts define database structures and analytical queries used during validation and reporting.